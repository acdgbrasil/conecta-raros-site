.env
.DS_Store
node_modules/
dist/
coverage/
*.log
superpowers/
.claude/

# Runtime config — gerado pelo entrypoint do container, nunca commitar
src/runtime-config.js

# Artefatos de auditoria/relatórios (não pertencem ao código de produção)
seo-strategy-*.html
